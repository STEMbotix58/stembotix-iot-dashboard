import DeviceStatusTable from "@/features/devices/components/DeviceStatusTable";

const DashboardDevicesSection = () => {
  return (
    <section>
      <DeviceStatusTable />
    </section>
  );
};

export default DashboardDevicesSection;
